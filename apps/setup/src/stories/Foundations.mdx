import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';

<Meta title="Foundations" />

# Foundations

SetUp's design tokens, translated from the Paper exploration (`setup-product-flow`) into `src/index.css`. This page renders the live CSS custom properties — if a swatch or size looks wrong, the tokens themselves are wrong, not this page.

## Color — Foundations v1

**Approved 2026-08-14.** The color section below is the canonical semantic source of truth for SetUp, defined by the user after a full audit of the current Paper file (6 screens: Home, Workout Overview, three Workout Mode states, Workout Completion). Paper remains the *visual* reference; this list is the *semantic* one. Don't infer, rename, or add color tokens without asking first.

### Primitives

Raw palette values. **Not exposed as Tailwind utilities** (`bg-moss` etc. don't exist) — components consume the semantic tokens below, never these directly, unless explicitly approved for a specific case.

<ColorPalette>
  <ColorItem title="moss" subtitle="--moss" colors={{ moss: '#294000' }} />
  <ColorItem title="lime" subtitle="--lime" colors={{ lime: '#BFF753' }} />
  <ColorItem title="lime-soft" subtitle="--lime-soft — not yet assigned to a semantic token" colors={{ 'lime-soft': '#CEFB83' }} />
  <ColorItem title="neutral-0 / neutral-50" subtitle="--neutral-0, --neutral-50" colors={{ 'neutral-0': 'var(--neutral-0)', 'neutral-50': 'var(--neutral-50)' }} />
</ColorPalette>

### Semantic tokens

<ColorPalette>
  <ColorItem
    title="background / background-inverse"
    subtitle="Page surface, and its full-bleed inversion (Workout Overview, Workout Completion)"
    colors={{ background: 'var(--background)', 'background-inverse': 'var(--background-inverse)' }}
  />
  <ColorItem
    title="foreground / foreground-secondary"
    subtitle="The one ink color for light-screen text, at two opacities — not separate hardcoded grays"
    colors={{ foreground: 'var(--foreground)', 'foreground-secondary': 'var(--foreground-secondary)' }}
  />
  <ColorItem
    title="foreground-inverse / foreground-inverse-secondary"
    subtitle="Same idea, for text on an inverted (dark) surface"
    colors={{ 'foreground-inverse': 'var(--foreground-inverse)', 'foreground-inverse-secondary': 'var(--foreground-inverse-secondary)' }}
  />
  <ColorItem
    title="surface-subtle / surface-inverse"
    subtitle="Barely-there tinted surfaces (stat chips, day-selector chip fills) — light and inverted-context versions"
    colors={{ 'surface-subtle': 'var(--surface-subtle)', 'surface-inverse': 'var(--surface-inverse)' }}
  />
  <ColorItem
    title="border / border-subtle"
    subtitle="Two weights: chip outlines (border) vs. dividers and other outlines (border-subtle)"
    colors={{ border: 'var(--border)', 'border-subtle': 'var(--border-subtle)' }}
  />
  <ColorItem
    title="primary / primary-foreground"
    subtitle="The real primary CTA fill: Start Workout, Save Activity, Next, Resume"
    colors={{ primary: 'var(--primary)', 'primary-foreground': 'var(--primary-foreground)' }}
  />
  <ColorItem
    title="interactive-subtle / interactive-subtle-inverse"
    subtitle="Always-visible (not hover-only) tinted button surface: Adjust plan, Pause, Discard Activity"
    colors={{ 'interactive-subtle': 'var(--interactive-subtle)', 'interactive-subtle-inverse': 'var(--interactive-subtle-inverse)' }}
  />
  <ColorItem
    title="state-active / state-inactive"
    subtitle="Step-bar segments and similar progress/selection indicators"
    colors={{ 'state-active': 'var(--state-active)', 'state-inactive': 'var(--state-inactive)' }}
  />
</ColorPalette>

**Two screens invert to a dark surface**: Workout Overview and Workout Completion are full-bleed moss with near-white text, instead of the usual light background — `bg-background-inverse text-foreground-inverse`. The same inversion gives the "Finish" button its dark fill on the otherwise-light Workout Mode screens.

### shadcn compatibility aliases — not canonical

`components/ui/button.tsx` (and any future shadcn-added component) expects `--secondary`, `--muted`, `--accent` (plus their `-foreground` pairs), `--card`, `--popover`, `--input`, `--ring`, and `--destructive` to exist, because shadcn's generated code references them. **None of these are part of Color Foundations v1** — they carry no approved product meaning yet. They're kept defined only so existing components don't break, conservatively aliased onto the semantic tokens above:

| shadcn variable | Aliased to | 
| --- | --- |
| `--secondary` / `--secondary-foreground` | `--surface-subtle` / `--foreground` |
| `--muted` / `--muted-foreground` | `--surface-subtle` / `--foreground-secondary` |
| `--accent` / `--accent-foreground` | `--interactive-subtle` / `--foreground` |
| `--card`, `--popover` (+ foregrounds) | `--background` / `--foreground` (flat surfaces, no distinct card tone) |
| `--input` | `--border` |
| `--ring` | `--foreground` |
| `--destructive` | unchanged shadcn default (no red exists in v1; Paper doesn't style Discard Activity as alarming) |

Don't use these six aliases intentionally in new SetUp components, and don't treat them as approved foundations — they're a compatibility shim, not a design decision. That's also why they're not shown as swatches above.

`--chart-1` through `--chart-5` are likewise unaddressed by v1 (the weekly-graph green that used to live in `--chart-1` no longer exists in Paper — replaced with moss) and are left at shadcn's neutral placeholders.

## Typography — Foundations v1

**Approved 2026-08-14**, from an exhaustive Paper audit and a cleanup pass that resolved every inconsistency it found (duplicate styles, same-role-different-values, stray one-offs — see the audit notes in this conversation's history if you need the reasoning). Font: the **system UI font stack** (`--font-sans`, Tailwind's built-in default — `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, …`), matching Paper's "System Sans-Serif" as a deliberate choice, not a webfont. (An earlier version of this page used Geist Variable, assuming Paper's value was an unset default — that assumption was wrong; the Geist dependency has been removed.) Hierarchy is driven primarily by **size**, not by introducing new weights — SetUp uses only 300/400/500, and each token bundles its size, weight, and line-height as one unit (a real Tailwind utility, e.g. `text-body` sets all three at once — not a size-only token you pair with a separate `font-*` class). Letter-spacing is `normal` on every token; none override it.

<div style={{ display: 'flex', flexDirection: 'column', gap: '16px', marginTop: '8px' }}>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-display-lg" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-display-lg</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>48px / weight 300 / 56px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-display-md" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-display-md</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>32px / weight 300 / 40px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-heading" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-heading</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>24px / weight 300 / 28px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-body" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-body</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>16px / weight 400 / 24px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-body-emphasis" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-body-emphasis</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>16px / weight 500 / 24px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-action" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-action</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>16px / weight 400 / 22px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-caption" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-caption</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>14px / weight 400 / 20px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-label" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-label</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>12px / weight 400 / 16px line-height</div>
    </div>
  </div>
  <div style={{ display: 'flex', alignItems: 'baseline', gap: '16px' }}>
    <span className="text-label-emphasis" style={{ color: 'var(--foreground)' }}>Aa</span>
    <div>
      <code>text-label-emphasis</code>
      <div style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>12px / weight 500 / 16px line-height</div>
    </div>
  </div>
</div>

| Token                 | Size | Weight | Line-height | Where it comes from in Paper |
| --------------------- | ---- | ------ | ------------ | ----------------------------- |
| `display-lg`          | 48px | 300    | 56px         | "102" stat (Home), hero title (Overview), hero timer (Completion) |
| `display-md`          | 32px | 300    | 40px         | Exercise titles, "Good Work!", big set-value numbers |
| `heading`              | 24px | 300    | 28px         | Section headers, "Sets" header, the live in-session counter |
| `body`                 | 16px | 400    | 24px         | The largest group — body copy, "Day N · body part" top-bars, list values |
| `body-emphasis`        | 16px | 500    | 24px         | "Hi Coral," greeting — the sole intentional emphasis moment |
| `action`               | 16px | 400    | 22px         | Button labels and stat-value chips (unified) |
| `caption`              | 14px | 400    | 20px         | "6 Exercices · 45-55 min" meta text (Home) |
| `label`                | 12px | 400    | 16px         | Field labels and Day/Set selector-chip labels (unified) |
| `label-emphasis`       | 12px | 500    | 16px         | Day-of-week letters on the weekly activity graph |

There is deliberately no separate numeric/timer/button/chip token — per the v1 rules, numeric content and component-specific text use the same hierarchy as everything else (`action` covers both button labels and stat-value chips; `heading` covers both section headers and the live timer; `display-lg` covers both the big stat number and hero timers). Don't create a `Button`/`Chip`/`Timer`-named token even if a future component seems to want its own size — ask first.

**Always pair `text-{token}` with `leading-{token}`** (e.g. `text-action leading-action`) — never `text-{token}` alone. Confirmed 2026-08-14: on form controls (`button`, `input`, etc.), Tailwind's own Preflight `font: inherit` reset wins the line-height cascade over `text-*`'s built-in `--text-*--line-height` fallback, so a bare `text-action` silently renders at the browser's inherited line-height (24px) instead of 22px. A real `leading-*` utility reliably overrides Preflight; the auto-paired one doesn't. Each of the 9 tokens above has a matching `--leading-*` value in `index.css` for exactly this reason.

## Spacing — Foundations v1

**Approved 2026-08-14**, from an exhaustive Paper audit and cleanup pass (unified the day/set column gap to 68px and the row-list gap to 12px across screens — see this conversation's history for the reasoning). Named tokens, not Tailwind's numeric scale — `gap-1`/`p-4` etc. are untouched and still mean whatever Tailwind's own arithmetic scale says; these are additive, collision-free utilities (`gap-space-5`, `p-space-7`, …).

<div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginTop: '8px' }}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-1</code>
    <div style={{ width: '4px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>4px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-2</code>
    <div style={{ width: '6px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>6px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-3</code>
    <div style={{ width: '8px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>8px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-4</code>
    <div style={{ width: '10px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>10px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-5</code>
    <div style={{ width: '12px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>12px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-6</code>
    <div style={{ width: '16px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>16px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-7</code>
    <div style={{ width: '20px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>20px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-8</code>
    <div style={{ width: '24px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>24px</span>
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
    <code style={{ width: '84px' }}>space-9</code>
    <div style={{ width: '40px', height: '16px', background: 'var(--primary)' }} />
    <span style={{ color: 'var(--foreground-secondary)', fontSize: '12px' }}>40px</span>
  </div>
</div>

| Token | Size | Where it comes from in Paper |
| --- | --- | --- |
| `space-1` | 4px | Label→value gaps (stat blocks, Reps/Weight, Overview field labels) |
| `space-2` | 6px | Button `paddingBlock`, chip padding, back-button padding |
| `space-3` | 8px | Related-content gaps (stats-header, "102"+"min", counter→"Time") |
| `space-4` | 10px | Stat-block `paddingBlock` |
| `space-5` | 12px | Row/chip padding, button icon-to-text gap, list-item spacing (workout-type rows, Set rows) |
| `space-6` | 16px | Button `paddingLeft`, gap between a button pair |
| `space-7` | 20px | Screen/section/card padding |
| `space-8` | 24px | Gaps between major sections |
| `space-9` | 40px | Overview's title→"What to expect" gap — a confirmed intentional outlier, not folded into `space-8` |

**68px is deliberately not a token.** It's the day/set-selector column's layout dimension (how far apart 3 stacked labels land, given the column's height and content), not an intentional spacing decision — treat it as a component measurement, not something to reach for when spacing something new.

## Radius — Foundations v1

**Approved 2026-08-14.** Only two canonical values exist — confirmed against Paper, where every non-pill surface measured at exactly 8px and every pill/circular element at 100px, with zero exceptions anywhere in the file.

<div style={{ display: 'flex', flexWrap: 'wrap', gap: '16px', marginTop: '8px' }}>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px' }}>
    <div style={{ width: '48px', height: '48px', background: 'var(--surface-subtle)', border: '1px solid var(--border-subtle)', borderRadius: 'var(--radius-md)' }} />
    <code style={{ fontSize: '12px' }}>radius-md</code>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px' }}>
    <div style={{ width: '48px', height: '48px', background: 'var(--surface-subtle)', border: '1px solid var(--border-subtle)', borderRadius: 'var(--radius-full)' }} />
    <code style={{ fontSize: '12px' }}>radius-full</code>
  </div>
</div>

- **`radius-md`** (8px) — cards, rows, chips, stat blocks. The one surface radius in the whole product.
- **`radius-full`** (100px) — buttons, circular icon buttons, pill-shaped dividers. In practice this is Tailwind's own `rounded-full` utility (hardcoded to a very large radius, not theme-configurable) — visually identical to 100px at every size used here, so there's no separate `rounded-radius-full` class; `--radius-full` exists purely as the documented canonical value.

**Do not create `radius-sm`, `radius-lg`, `radius-xl`, or any other radius token** unless Paper introduces an actual new radius — there are only two surface treatments in this product, pill or `radius-md`, nothing in between.

**Update 2026-08-14**: `--radius-sm`/`--radius-lg`/`--radius-xl`/`--radius-2xl`/`--radius-3xl`/`--radius-4xl` (and their `--radius` seed) have been removed from `index.css`. They were legacy shadcn scaffold values kept only because Button v1's predecessor referenced `--radius-lg`; once Button v1 (see "Buttons" below) stopped using them, a full `src/` grep confirmed zero remaining consumers, so they were deleted rather than left as dead code.

## Borders

Width follows Tailwind's default scale (`border` = 1px, `border-2` = 2px) — not overridden. Two color weights, both semantic tokens: `border` (moss @ 20%) for chip outlines, `border-subtle` (moss @ 10%) for dividers and other outlines.

Paper's dividers are filled rectangles (`background-color: var(--border-subtle)` at a fixed height), not CSS borders. The day-selector chips use a real `outline` (not a fill, not a `border`) at the stronger weight — `outline: 1px solid var(--border)` — so it doesn't affect layout box size.

<div style={{ display: 'flex', gap: '16px', marginTop: '8px' }}>
  <div style={{ width: '80px', height: '48px', border: '1px solid var(--border-subtle)', borderRadius: 'var(--radius-md)' }} />
  <div style={{ width: '48px', height: '48px', outline: '1px solid var(--border)', borderRadius: '9999px' }} />
</div>

## Shadows — Foundations v1

**Approved 2026-08-14.** One token, one consumer: `--shadow-button` (`0 4px 8px 2px`, tinted at `border-subtle`'s weight — moss @ 10%) is applied to Button v1's `inverse` variant only (`components/ui/button.tsx`) — Finish is the sole button in Paper that carries a shadow, confirmed by the full button audit and re-audit in this conversation. `primary`, `ghost`, and `ghost-inverse` have no shadow.

<div style={{ display: 'flex', gap: '24px', marginTop: '8px', alignItems: 'center' }}>
  <div style={{ padding: '12px 24px', borderRadius: '9999px', background: 'var(--background-inverse)', color: 'var(--foreground-inverse)', boxShadow: 'var(--shadow-button)' }}>Finish</div>
</div>

Don't add a shadow anywhere else — other buttons, cards, sections — without a new Paper example and explicit approval, the same way this one was.

## Iconography

Library: **lucide-react** only (spec §14) — never emoji as icons.

Sizing is established by `components/ui/button.tsx` (Button v1): `size-5` (20px) is the default for icons inside a button, matching the exact icon dimensions measured across every button in Paper (every icon SVG is 20×20). Outside of Button, follow the same convention — `size-5` (20px) as the default, sizing down only when a specific inline-with-text context calls for it.

**Stroke width is `1.5`, not Lucide's default of `2`.** Corrected 2026-08-14 — the Button audit read Paper's raw icon markup directly (`get_jsx`, not just measured dimensions) and found every stroke-based icon uses `strokeWidth="1.5"`. An earlier version of this page said to leave Lucide's default alone; that was an unverified assumption, not a Paper measurement. Button v1 enforces this with `[&_svg]:[stroke-width:1.5]` on its base classes — outside Button, set it explicitly per icon until something more central exists.

### Stroke vs. fill — approved 2026-08-14

**Default icon** — `stroke="currentColor"`, `fill="none"` (Lucide's own default `fill`; don't override it). Color comes entirely from `currentColor`, so an icon sitting inside a colored button automatically picks up the right ink — Button never sets an icon color directly, and never will (no filled Button variants, no filled-icon color tokens, no component-specific icon colors). (Paper itself has no explicit color set on any icon yet — `currentColor` is a codebase convention, not something measured in Paper.)

**Filled workout action icon** (an approved, named exception) — `stroke="none"`, `fill="currentColor"`. This is an *icon-level* choice made where the icon is used, not a Button prop or variant — each approved filled icon decides for itself. Exactly three icons carry this treatment today:

| Icon | Lucide component | Used for |
| --- | --- | --- |
| Play | `Play` | Start Workout, Resume |
| Pause | `Pause` | Pause |
| Finish | `Square` | Finish |

```tsx
<Play data-icon="inline-start" fill="currentColor" stroke="none" />
<Pause data-icon="inline-start" fill="currentColor" stroke="none" />
<Square data-icon="inline-start" fill="currentColor" stroke="none" />
```

**Both props are required, not just `fill`.** Every Lucide icon component ships `stroke="currentColor"` and `stroke-width="2"` on its root `<svg>` regardless of what `fill` you pass — adding `fill="currentColor"` alone leaves a 2px outline stroke doubled on top of the fill, which Paper's equivalent icons don't have (confirmed via `get_jsx`: Paper's filled icons carry a `fill` attribute and *no* `stroke` attribute at all). `stroke="none"` removes that leftover outline.

Don't extend this treatment to any other icon without adding it to this table first — it's a short, named list, not a general rule for "icons that feel like actions."

This is a usage rule, not a gallery — see [lucide.dev/icons](https://lucide.dev/icons) for what's available. The rule that matters for SetUp is *when* an icon is allowed to carry meaning on its own. Per spec §16, Workout Mode has to work for someone standing, moving, and tired mid-session:

- Icon-only controls are reserved for a small set of very common, instantly-recognizable actions (back, close, pause).
- Everything else — and anything in Onboarding, Plan Settings, or a primary call-to-action — pairs the icon with a visible text label. Don't let an icon carry meaning alone if getting it wrong costs the user their workout state.

## Buttons — Button v1, implemented

**Implemented 2026-08-14** in `components/ui/button.tsx`, replacing the prior shadcn scaffold (`default`/`outline`/`secondary`/`ghost`/`destructive`/`link`) entirely. Four variants, matching the three visual tiers found across all 10 button instances audited in Paper (one tier splits by light/dark screen context):

| Variant | Background | Text | Paper examples |
| --- | --- | --- | --- |
| `primary` | `--primary` | `--primary-foreground` | Start Workout, Save Activity, Next, Resume |
| `ghost` | `--interactive-subtle` | `--foreground` | Adjust plan, Pause |
| `inverse` | `--background-inverse` | `--foreground-inverse` | Finish |
| `ghost-inverse` | `--interactive-subtle-inverse` | `--foreground-inverse` | Discard Activity |

No `size` prop — every Paper button is one size. Padding is icon-aware via the `data-icon="inline-start"|"inline-end"` convention on icon children (see `button.stories.tsx`): no icon → `space-7` (20px) both sides; leading icon → `space-6`/`space-7` (16/20); trailing icon → `space-7`/`space-6` (20/16). Vertical padding `space-2` (6px), icon/text gap `space-5` (12px), radius `radius-full`, typography `action`. Height is a fixed `48px` (`h-12`) — approved 2026-08-14 as a direct exception to the spacing scale (no `space-*` token covers 48px); content centers in the extra space above/below the 22px line-height via `items-center`, matching Paper exactly rather than the ~34px a padding-only, content-driven height would give. Back/Settings icon-only controls are explicitly **not** part of Button v1 — no `IconButton` yet. No hover/pressed/disabled/loading visual states were invented — Paper shows exactly one resting state per button.

**Resolved 2026-08-14**: `--shadow-button` approved as Shadow Foundations v1 and applied to `inverse` only — see "Shadows" above.

## Source of truth

These values were measured directly from the Paper file via `get_computed_styles` on real nodes across all 6 current screens, not eyeballed from a screenshot. The Color section reflects **Color Foundations v1**, explicitly approved by the user — treat it as fixed unless the user revises it, even if Paper drifts further. Every other section (typography, spacing, radius, borders, shadows, iconography) still tracks Paper directly and should be re-audited if a meaningful amount of time has passed.
